enum Size {
  SMALL
  REGULAR
}

enum CoffeeType {
  CAPPUCCINO
  FLAT_WHITE
  ESPRESSO
}

type Coffee {
  id: String!
  size: Size!
  name: String!
  type: CoffeeType!
}

type Query {
  hello: String!
  foundAnimals: [Int]!
}

type Mutation {
  sendImage(image: String!): Int}